export default {
  /**
   * The API key for this application's Apollo Engine
   * service (which can be found under the Settings
   * menu on <https://engine.apollographql.com>.
   *
   * Leave blank to disable proxy & middleware.
   *
   * @type {String}
   */
  key: process.env.APOLLO_ENGINE_API_KEY,
};
